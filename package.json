{
  "name": "eslint-config",
  "private": true,
  "scripts": {
    "test": "ava",
    "install": "rm -rf packages/*/package-lock.json; lerna clean --yes; lerna bootstrap --no-ci",
    "bump": "node scripts.js --task=bump",
    "publish": "node scripts.js --task=publish"
  },
  "devDependencies": {
    "@absolunet/manager": "0.1.0",
    "@absolunet/tester": "2.4.0",
    "glob": "7.1.3",
    "lerna": "3.4.3"
  }
}
